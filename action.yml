name: jsAction
description: calculator to test JavaScript actions
inputs:
  number1:
    required: true
    default: 0
  number2:
    required: true
    default: 0
outputs:
  resullt:
    description: "output of the sum of the 2 numbers"
runs:
  using: "node20"
  main: "dist/index.js"
